# تأميني - لوحة التحكم

## 📋 نظرة عامة

لوحة تحكم حديثة لموقع تأميني لاستقبال وعرض بيانات العملاء بطريقة منظمة.

## 🚀 الميزات

- ✅ **API متكامل** لاستقبال البيانات
- ✅ **قاعدة بيانات SQLite** لحفظ البيانات
- ✅ **لوحة تحكم تفاعلية** بواجهة عربية
- ✅ **إحصائيات فورية** في الوقت الفعلي
- ✅ **بحث وفلترة** متقدمة
- ✅ **تصميم متجاوب** للجوال

## 📊 API Endpoints

### استقبال البيانات
```
POST /api/step1     # البيانات الأساسية
POST /api/step2     # بيانات التأمين  
POST /api/final     # البيانات النهائية
```

### جلب البيانات
```
GET /api/submissions        # جميع الطلبات
GET /api/submissions/status/:status  # فلترة بالحالة
GET /api/stats             # الإحصائيات السريعة
```

## 🌐 الروابط

- **الصفحة الرئيسية**: `/`
- **لوحة التحكم**: `/dashboard.html`
- **معلومات API**: `/api-status`

## 🛠️ النشر على Render

### إعدادات النشر:
```
Environment: Node
Build Command: npm install
Start Command: npm start
```

### متطلبات النظام:
- Node.js 18+
- SQLite 3

## 📝 مثال الاستخدام

```javascript
// إرسال بيانات الخطوة الأولى
fetch('/api/step1', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    userName: 'أحمد محمد',
    phoneNumber: '0501234567',
    idNumber: '1234567890',
    offerType: 'new',
    regType: 'serial',
    serialNumber: 'ABC123',
    carYear: '2020'
  })
});
```

## 📧 الدعم

لأي استفسارات أو مشاكل، تواصل معنا.

---

**تم إنشاء هذا المشروع بواسطة MiniMax Agent**